4.15 Identify and correct the errors in each of the following pieces of code. [Note: There may be
more than one error in each piece of code.

a) if (age >= 65);
System.out.println("Age is greater than or equal to 65");
else
System.out.println("Age is less than 65)";
Answer= Error is, no curly braces {} to show block of code

b) int x == 1, total == 0;
while (x <= 10) {
total ++x;
System.out.println(x);
}
Answer= Error is, wrong variable declaration and assignment operator

c) while (x <= 100)
total += x;
++x;
Answer= Error is, no curly braces to indicate block

d) while (y =! 0)
{
System.out.println (y);
Answer= Error is, wrong tenary operator, the correct one is != 
Also no right curly brace to indicate end of block 
